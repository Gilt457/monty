#include "monty.h"
/**
 * f_nop - does nothing
 * @head: pointer to the top of the stack
 * @counter: line number
 * Return: void
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
